# Echon — Backlog

Items here are explicitly parked. Do NOT build unless the user asks.

## Active backlog (post-pivot)

| Item | Description | Build Trigger | Status |
|------|-------------|---------------|--------|
| Step 11 — number provisioning polish | Vapi `provider: "vapi"` flow works but rejects many area codes (e.g. 415 unavailable, suggested 949/878/540). Decide between (a) sticking with Vapi-managed numbers + showing available-area-codes hint inline, or (b) BYO Twilio import for full area-code coverage. Plus: auto-fill area code from `business_address` via geocoding so non-tech users don't have to type one. | Pre-pilot — needed before first paying client | Parked 2026-05-04 |
| Step 10 — Google Calendar OAuth | Real OAuth + calendar picker + 2-week look-ahead preview. Currently placeholder. Hard-blocked on GCP project + verified consent screen (see URGENT.md). | GCP verification complete | Parked 2026-05-04 |
| Step 10 — Multi-provider calendar connect (Google / Outlook / phone) | "Connect your calendar" should not be Google-only. Onboarding Step 10 + Settings → Integrations need to offer three options: (a) Google Calendar (OAuth — covered by the GCP verification item above), (b) Outlook / Microsoft 365 (Microsoft Graph OAuth), and (c) phone calendar (iOS/Android local) — likely an `.ics` subscription URL the customer pastes into their phone, since native phone calendars don't expose a write API to a web app. Needs: provider picker UI in Step 10, per-provider OAuth flows, an `integrations.calendar_provider` column to remember which one the workspace picked, and a normalized free/busy + create-event interface so the booking tool doesn't branch per-provider. | Pre-pilot — many HVAC owners run on Outlook or only their phone | Parked 2026-05-06 |
| Concierge call scheduler in Step 11 | Calendly-style embed for booking the 5-min forwarding setup call (per `_wireframes/onboarding.md`). Independent of Vapi. | Pre-pilot | Parked 2026-05-04 |
| Theme switcher in Settings → Appearance | System/Dark/Light picker. Provider already mounted (`next-themes`, default = system); just need the UI in Settings. | Settings page build | Parked 2026-05-04 |
| Realtime dashboard updates | Supabase realtime channels so new calls appear without manual refresh. Deferred for MVP — manual refresh button is enough until pilot clients ask for it. | Pilot feedback | Parked 2026-05-04 |
| Developer / admin panel | Separate internal tool (per CLAUDE.md, NOT a view inside the client dashboard). Candidate widgets: all-workspaces overview, system health (calls/day, Vapi cost, webhook errors), onboarding funnel, recent signups, flagged-calls inbox, pilot impersonation. | Multiple pilot clients | Parked 2026-05-04 |
| Onboarding — service radius | Add a step (or extend the business-address step) to capture a service radius in miles around the business ZIP. Used at call time: if the caller's address falls outside the radius, the AI says "we don't service that area" and escalates to a human (operator may still want the odd job). Needs: numeric input with sensible default (e.g. 50mi), geocoding of caller address, distance check tool wired into the agent. | Pre-pilot | Parked 2026-05-05 |
| Vapi agent prompt — escalate on rejection | When a customer rejects the offered appointment slot ("that's not soon enough", "I need someone today", etc.), the agent should escalate to a human rather than ending the call no_action. Currently the agent treats the rejection as the end of the conversation. Add a behavioral rule + an `escalate_to_human` tool call trigger to the agent prompt; verify with a test call that says "tomorrow is too late" and confirms escalation fires. | Phase 4 — agent prompt tuning | Parked 2026-05-05 |
| Notify operators on case assignment | When an operator is assigned to a case slot (CS rep / technician / manager) — manually via the dropdown or via the Auto-assign confirm — fire an email to `operators.email` and a text message to `operators.phone`. Both contact fields already exist on the table. Needs: (a) transactional email provider (Resend/Postmark) — same one we use for general transactional email per the open SMTP action item, (b) SMS provider (Twilio Programmable Messaging or Vapi-managed equivalent), (c) a notification template per channel including customer name + case title + role they were assigned, (d) hook into `assignCaseSlot` and `confirmAutoAssign` server actions, (e) per-operator opt-out toggle in Settings → Team. | Pre-pilot — once SMTP is wired and there's an SMS budget | Parked 2026-05-06 |
| Subscription tiers — gate "Recommended action" behind higher tier | The AI-generated recommended action on the call detail page should NOT be available to every paying client — it's a higher-tier feature. Today it ships to all users while we get pilots running and learn what features people actually pay for. Once pilot feedback validates a tier structure, build: (a) a `subscription_tier` enum on `workspaces` (or similar), (b) a feature-gate helper in `src/lib/`, (c) wrap the recommended-action card + its `generateRecommendedAction` server action with the gate, (d) upgrade-prompt UI for lower tiers. Other features likely to share the same gate should be migrated together so we ship one tier system, not piecemeal flags. | Post-pilot — once tier structure is decided based on pilot client feedback | Parked 2026-05-06 |
| Role-based workspace access (Manager / CSR / Tech) | Within a single Client workspace, the manager who signs up should be able to invite teammates with scoped permissions instead of every signed-in user seeing the same dashboard. Three roles: **Manager** = full control (settings, billing, AI config, all customer + case data, can assign and reassign anyone). **Customer service rep** = can resolve cases the AI didn't auto-resolve, view + edit customer data, dismiss "Needs attention" items, but no settings/billing/AI config. **Tech** = read-only view of the case they're on (who else is on it, job details, customer notes, equipment, address) — no edits, no other customers. Each role connects their own calendar (Google / Outlook / native phone — see related calendar item) so the AI can route bookings to the right person's availability. Needs: (a) `workspace_members` table with a `role` enum, (b) invite flow + join via signed link, (c) middleware/route gates per role, (d) per-role nav (techs only see Schedule + the cases they're on), (e) RLS policies that read the member's role, not just workspace ownership. **Before building, ask which surfaces each role sees first** — manager UX is mostly done, CSR and Tech need wireframes. | Pre-pilot — needed once any pilot has more than one person using it | Parked 2026-05-06 |
| Beta-test outreach — car detailing buddy | Reach out to a friend who owns a car detailing business and ask if he'd like to beta test Echon on his customer cars. Different vertical from HVAC (the launch trade), but the voice receptionist + booking + after-hours triage workflows generalize cleanly to detailing — would also be a useful signal on whether the trade-aware action plan prompt and `business_type` enum hold up outside HVAC. Action: send the message, capture his feedback in `_project/PILOT_FEEDBACK.md` (create if needed), and if he agrees: add `detailing` to the `business_type` enum, scope a minimal pilot, and confirm onboarding copy reads sensibly for a non-HVAC trade. | When ready to recruit a second-vertical pilot | Parked 2026-05-06 |
| Capture "Other" service requests for trade expansion | When a caller asks for a service the agent doesn't have configured and ends up in an "other / unknown" bucket, log the raw service text plus the trade context so the user can review it later and either (a) add it to the workspace's service catalog or (b) decide it's a new trade vertical worth supporting and curate AI prompts/tools for it. Today the call summary captures it incidentally; this would surface it as first-class data — a `calls.requested_service_other text` column populated by the post-call extractor when no catalog match, plus a "Service requests we couldn't handle" admin view that groups by raw text + frequency. Useful both for per-workspace catalog growth and for picking the next vertical (detailing, plumbing, etc.) based on actual demand. | Post-pilot — once enough call volume exists to see patterns | Parked 2026-05-06 |
| Customer support email + intake | Echon needs a real support address for customer-facing flows that say "contact support" — first user is the new "need more than one phone number? Contact support" copy in Settings → Connections, but the same address will be used for billing issues, account recovery, calendar OAuth failures, and pilot feedback. Action: (a) decide on the address (e.g. `support@echonai.com` once a domain is registered, or a Gmail alias as a stop-gap), (b) point the address at a real inbox someone (you) reads daily, (c) replace every hardcoded "contact support" string in the codebase with a constant referencing this address, (d) consider Help Scout / Front / a shared Gmail label so multiple people can triage when the team grows. **Blocks:** the "more than one phone number" Settings copy, the "calendar disconnected" recovery, and any first-pilot crisis where the client needs to reach a human. | Pre-pilot — first paying client will need this immediately | Parked 2026-05-07 |
| Settings panel for Recommended action | Today the AI-generated "Recommended action" briefing is a one-click feature on the case detail page with sensible defaults — no settings UI. Punted because there's no clear demand: the prompt is well-tuned per trade, the bullets are short, and clients can re-generate freely. Revisit if pilot feedback asks for: tone customization (formal / casual / blunt), max-length controls, opt-out per workspace, custom system prompt overrides, or per-trade prompt fine-tuning. Lives separately from the **subscription tiering** backlog item — that one decides *who* can use the feature; this one decides whether *configuring* the feature is worth a UI. **Don't build pre-emptively.** | Pilot feedback explicitly asks for it | Parked 2026-05-07 |
| Editable on-call number in Settings | The on-call number(s) collected in onboarding Step 7 / Step 9 (after-hours + escalation routing — `agent_configs.oncall_numbers`) should be editable in Settings → After-hours & escalation post-onboarding. Today it's only set during onboarding; if the client adds a tech, swaps phones, or rotates who's on call, they have nowhere to update it. Needs: a list editor in the existing Settings → After-hours section (add / edit / remove entries with `phone`, `label`, optional `rotation_window`), a server action that updates `agent_configs.oncall_numbers` and re-syncs the Vapi assistant via `_voice-sync.ts`, and validation that at least one number remains active when after-hours mode is `escalate` or `live_transfer`. | Pre-pilot — first pilot client will hit this immediately | Parked 2026-05-07 |
| Privacy + data-handling onboarding tab | Add a dedicated privacy step to the onboarding wizard that explains, in plain language, what Echon collects and how it's stored: call recordings + transcripts (where they live, retention, who can access), customer PII (names / phone / address / equipment notes), recording-disclosure requirements per US state (two-party-consent states need the agent's greeting to include disclosure — auto-injected today, but the user should know), Anthropic transcript processing for summarization, GCal / integration scopes, and a clear "your data, your workspace" boundary (RLS isolation). Should also surface the workspace's own privacy posture: a toggle for call-recording on/off, retention slider (e.g. 30 / 90 / 365 days), and a "download all my data" / "delete everything" pointer to Settings → Account. Tied to the second concern below — the goal is no surprises about what gets stored or sent to a third party. **Plus:** before/while building, ask the claude-code-guide agent how to harden the dev side too — what in `.claude/` (commands, skills, settings) can leak sensitive context if pushed to a public repo, and what should be gitignored vs. shared. Two surfaces, same theme: data handling that the user (client owner + repo owner) can trust. | Pre-pilot — privacy clarity is a credibility prerequisite for the first paying client | Parked 2026-05-07 |

## Archived — pre-pivot direction (Instagram DM setter)

These items were scoped under the original Echon product (AI Instagram DM
setter for coaches/agencies). The product pivoted on 2026-05-01 to an AI
voice receptionist for HVAC. Items below are preserved for historical
context only and are NOT to be built.

| Item | Description | Build Trigger | Status |
|------|-------------|---------------|--------|
| Follower-based queue | Prioritize leads based on follower count or account size | Agency tier scale | Archived 2026-05-01 |
| Calendly webhook | Detect booking confirmation via Calendly webhook instead of keyword | 10+ clients | Archived 2026-05-01 |
| Bot / spam deletion | Allow users to delete conversations identified as bots or spam accounts; currently no delete functionality exists | Data bloat complaints | Archived 2026-05-01 |
| Multi-account support | Connect and manage more than one Instagram account per workspace | Agency tier scale | Archived 2026-05-01 |
| Multiple active offers | Allow users to run more than one offer/lead magnet at the same time | Post-MVP client demand | Archived 2026-05-01 |
| Instagram Meta API integration | Connect Instagram DMs via Meta Graph API + OAuth | Pre-launch (imperative) | Archived 2026-05-01 |
| Workflows section | Dedicated page for adding lead magnets, defining trigger keywords, and building follow-up sequences | Post-MVP or early build | Archived 2026-05-01 |
| WhatsApp integration | Connect WhatsApp DMs as a second messaging channel via WhatsApp Business API | Post-MVP | Archived 2026-05-01 |
| iMessage integration | Connect iMessage as a messaging channel — feasibility TBD | Research needed | Archived 2026-05-01 |
| Calendly / Cal.com integration | Detect booking confirmations automatically via webhook | 10+ clients | Archived 2026-05-01 |
| Email / push notifications | Notify users of urgent leads, bookings, and AI flags via email or push | User demand | Archived 2026-05-01 |
| Subscription tier definitions (DM-setter version) | Solo / Premium / Enterprise tiers scoped around Manual/Hybrid/Auto modes and DM volume | Pre-launch | Archived 2026-05-01 |
